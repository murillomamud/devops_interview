
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 3000
    - name: metrics
      port: 9464
      targetPort: 9464
